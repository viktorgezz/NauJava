# Используем официальный образ Node.js
# Выбираем версию, соответствующую требованиям проекта (^20.19.0 || >=22.12.0)
FROM node:20-alpine

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем файлы зависимостей отдельно для кеширования слоя
# Это позволяет Docker переиспользовать слой с node_modules,
# если package.json и package-lock.json не изменились
COPY package.json package-lock.json ./

# Устанавливаем зависимости
# npm ci быстрее npm install и гарантирует установку из lock файла
RUN npm ci

# Копируем остальные файлы проекта
COPY . .

# Открываем порт для dev сервера
EXPOSE 5173

# Запускаем dev сервер с hot-reload
CMD ["npm", "run", "dev", "--", "--host"]

