# NauJava - Система тестирования

Полнофункциональная веб-система для создания, редактирования и прохождения тестов с автоматической проверкой результатов.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Как работает система](#как-работает-система)
- [Запуск проекта](#запуск-проекта)
- [Конфигурация](#конфигурация)
- [API](#api)

## 🎯 Описание проекта

**NauJava** - это система управления тестами, которая позволяет:
- Создавать и редактировать тесты с различными типами вопросов
- Проходить тесты онлайн
- Автоматически проверять ответы и выставлять оценки
- Просматривать результаты прохождения тестов
- Управлять пользователями и правами доступа

## 🏗️ Архитектура

Проект построен по архитектуре **клиент-сервер** с использованием микросервисного подхода через Docker Compose:

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │
       │ HTTP/HTTPS
       ▼
┌─────────────┐
│    Nginx    │ ← Reverse Proxy (порт 80)
└──────┬──────┘
       │
       ├─────────────────┬──────────────────┐
       │                 │                  │
       ▼                 ▼                  ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  Frontend   │  │   Backend   │  │  PostgreSQL │
│  (Vue 3)    │  │ (Spring Boot)│  │   (DB)      │
│  :5173      │  │    :8080     │  │   :5432     │
└─────────────┘  └──────┬───────┘  └─────────────┘
                        │
                        │ JDBC
                        ▼
                ┌─────────────┐
                │  PostgreSQL │
                │   Database  │
                └─────────────┘
```

### Компоненты системы

1. **Frontend (Vue 3 + Vite)**
   - SPA приложение на Vue 3
   - Использует Pinia для управления состоянием
   - Vue Router для маршрутизации
   - Axios для HTTP запросов

2. **Backend (Spring Boot)**
   - REST API на Spring Boot 3.5.6
   - Spring Security с JWT аутентификацией
   - Spring Data JPA для работы с БД
   - PostgreSQL в качестве базы данных

3. **Nginx**
   - Reverse proxy для маршрутизации запросов
   - Проксирование API запросов на backend
   - Проксирование статических файлов и SPA на frontend
   - Решает проблемы CORS, проксируя все через один домен

4. **PostgreSQL**
   - Реляционная БД для хранения всех данных

## 🛠️ Технологический стек

### Backend
- **Java 21** - язык программирования
- **Spring Boot 3.5.6** - фреймворк
- **Spring Security** - безопасность и аутентификация
- **Spring Data JPA** - работа с БД
- **PostgreSQL 18.0** - база данных
- **JWT (jjwt 0.12.6)** - токены аутентификации
- **Maven** - управление зависимостями
- **OpenAPI/Swagger** - документация API

### Frontend
- **Vue 3** - JavaScript фреймворк
- **Vite 7.2.4** - сборщик и dev сервер
- **Pinia** - state management
- **Vue Router** - маршрутизация
- **Axios** - HTTP клиент
- **Vee-Validate** - валидация форм

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Nginx** - reverse proxy
- **Maven** - сборка Java проекта

## 📁 Структура проекта

```
NauJava/
├── src/                          # Backend (Java/Spring Boot)
│   ├── main/
│   │   ├── java/
│   │   │   └── ru/viktorgezz/NauJava/
│   │   │       ├── auth/        # Аутентификация и регистрация
│   │   │       ├── domain/      # Доменные модели и сервисы
│   │   │       │   ├── test/    # Тесты
│   │   │       │   ├── question/# Вопросы
│   │   │       │   ├── result/  # Результаты прохождения
│   │   │       │   ├── user/    # Пользователи
│   │   │       │   └── ...
│   │   │       ├── security/    # Spring Security конфигурация
│   │   │       └── handler/     # Обработка ошибок
│   │   └── resources/
│   │       └── application.yml  # Конфигурация Spring Boot
│   └── test/                    # Тесты
├── frontend/
│   └── testing-system/          # Frontend (Vue 3)
│       ├── src/
│       │   ├── api/             # API клиенты
│       │   ├── components/      # Vue компоненты
│       │   ├── views/           # Страницы
│       │   ├── stores/          # Pinia stores
│       │   └── router/          # Маршруты
│       └── package.json
├── nginx/
│   └── nginx.conf               # Конфигурация Nginx
├── docker-compose.yml           # Оркестрация контейнеров
├── Dockerfile                   # Dockerfile для backend
└── pom.xml                      # Maven конфигурация
```

## 🔄 Как работает система

### 1. Аутентификация и авторизация

Система использует **JWT (JSON Web Tokens)** для аутентификации:

1. **Регистрация/Вход:**
   - Пользователь регистрируется или входит через `/auth/register` или `/auth/login`
   - Backend генерирует пару токенов:
     - **Access Token** (10 минут жизни) - для доступа к API
     - **Refresh Token** (100 минут жизни) - для обновления access token

2. **Авторизация запросов:**
   - Frontend сохраняет токены в localStorage
   - При каждом запросе к API добавляется заголовок `Authorization: Bearer <access_token>`
   - `JwtAuthenticationFilter` проверяет токен на каждом запросе

3. **Обновление токенов:**
   - При истечении access token, frontend автоматически запрашивает новый через `/auth/refresh`
   - Если refresh token истек, пользователь перенаправляется на страницу входа

### 2. Создание и редактирование тестов

**Процесс создания теста:**

1. Пользователь нажимает "Создать тест" на главной странице
2. Заполняет метаданные теста (название, описание)
3. Добавляет вопросы с вариантами ответов:
   - **SINGLE_CHOICE** - выбор одного ответа
   - **MULTIPLE_CHOICE** - выбор нескольких ответов
   - **OPEN_TEXT** - текстовый ответ
4. Сохраняет тест через API `/tests/metadata` и `/tests/content`

**Редактирование:**
- Можно редактировать через UI или JSON редактор
- Поддерживается как визуальное редактирование, так и прямой JSON

### 3. Прохождение теста

1. Пользователь выбирает тест из списка
2. Переходит на страницу прохождения (`/tests/:id/pass`)
3. Отвечает на вопросы
4. Отправляет ответы через API `/results`
5. Backend:
   - Проверяет ответы
   - Выставляет оценку
   - Сохраняет результат в БД
   - Возвращает результат с детальной информацией

### 4. Просмотр результатов

- Пользователь может просмотреть все свои результаты
- Детальная информация о прохождении:
  - Общая оценка
  - Детали по каждому вопросу
  - Правильные/неправильные ответы
  - Пояснения к вопросам

### 5. Маршрутизация запросов (Nginx)

Nginx работает как единая точка входа:

- **API запросы** (`/auth/*`, `/tests/*`, `/results/*`, `/users/*`) → проксируются на `backend:8080`
- **Swagger** (`/swagger-ui/*`, `/v3/api-docs/*`) → проксируются на `backend:8080`
- **Мониторинг** (`/monitoring/*`) → проксируются на `backend:8080`
- **Все остальные запросы** → проксируются на `frontend:5173` (Vite dev server)

Это решает проблему CORS, так как все запросы идут через один домен.

### 6. Работа с базой данных

**Основные сущности:**
- `User` - пользователи системы
- `TestModel` - тесты
- `Question` - вопросы теста
- `AnswerOption` - варианты ответов
- `Result` - результаты прохождения тестов
- `UserAnswer` - ответы пользователя на вопросы

**Связи:**
- Тест принадлежит пользователю (автор)
- Тест содержит множество вопросов
- Вопрос содержит варианты ответов (для SINGLE_CHOICE/MULTIPLE_CHOICE)
- Результат связан с тестом и пользователем
- Результат содержит множество ответов пользователя

## 🚀 Запуск проекта

### Требования

- Docker и Docker Compose
- Java 21 (для локальной разработки backend)
- Node.js 20.19.0+ или 22.12.0+ (для локальной разработки frontend)
- Maven 3.9+ (для локальной разработки backend)

### Быстрый старт с Docker Compose

1. **Клонируйте репозиторий**

2. **Создайте файл `.env` в корне проекта:**

```env
POSTGRESQL_USERNAME=postgres
POSTGRESQL_PASSWORD=your_strong_password
POSTGRESQL_DB=testing_system
SECURITY_ORIGIN_FRONTED=http://localhost
SECURITY_ALLOWED_ORIGINS=http://localhost,http://localhost:80
VITE_BACKEND_API_URL=
```

3. **Запустите все сервисы:**

```bash
docker-compose up -d
```

4. **Откройте в браузере:**

- Frontend: http://localhost
- Backend API: http://localhost:8080
- Swagger UI: http://localhost/swagger-ui.html

### Остановка

```bash
docker-compose down
```

### Пересборка контейнеров

```bash
docker-compose down
docker-compose build
docker-compose up -d
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# PostgreSQL
POSTGRESQL_USERNAME=postgres
POSTGRESQL_PASSWORD=your_password
POSTGRESQL_DB=testing_system

# Security (CORS)
SECURITY_ORIGIN_FRONTED=http://localhost
SECURITY_ALLOWED_ORIGINS=http://localhost,http://localhost:80

# Frontend
VITE_BACKEND_API_URL=  # Пустая строка = относительные пути через nginx
```

### Для продакшена на сервере

```env
SECURITY_ORIGIN_FRONTED=http://your-domain.com
SECURITY_ALLOWED_ORIGINS=http://your-domain.com,http://www.your-domain.com
VITE_BACKEND_API_URL=  # Оставить пустым для работы через nginx
```

### Backend конфигурация (`application.yml`)

Основные настройки находятся в `src/main/resources/application.yml`:

- **JWT токены:**
  - Access token: 10 минут (600000 мс)
  - Refresh token: 100 минут (6000000 мс)

- **База данных:**
  - Настраивается через переменные окружения
  - Hibernate ddl-auto: `update` (автоматическое обновление схемы)

## 📡 API

### Публичные эндпоинты (без аутентификации)

- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему
- `POST /auth/refresh` - Обновление access token

### Защищенные эндпоинты (требуют JWT токен)

#### Тесты
- `GET /tests` - Список тестов (с пагинацией)
- `GET /tests/{id}` - Получить тест для прохождения
- `GET /tests/metadata?id={id}` - Метаданные теста
- `GET /tests/content?id={id}` - Содержимое теста (для редактирования)
- `POST /tests/metadata` - Создать/обновить метаданные теста
- `PUT /tests/content` - Обновить содержимое теста
- `PUT /tests/content/json` - Обновить содержимое теста (JSON)
- `DELETE /tests/{id}` - Удалить тест

#### Результаты
- `POST /results` - Отправить ответы на тест
- `GET /results/{id}` - Получить результат прохождения
- `GET /results` - Список результатов текущего пользователя

#### Пользователи
- `GET /users/me` - Информация о текущем пользователе

### Документация API

После запуска проекта доступна Swagger документация:
- http://localhost/swagger-ui.html
- http://localhost/v3/api-docs

## 🔒 Безопасность

- **JWT аутентификация** - stateless аутентификация через токены
- **BCrypt** - хеширование паролей
- **CORS** - настроен для безопасного взаимодействия frontend и backend
- **Spring Security** - защита эндпоинтов
- **Валидация данных** - на уровне DTO через Jakarta Validation

## 🧪 Тестирование

Backend содержит unit и integration тесты:

## 📝 Особенности реализации

### Frontend

- **Автоматическое обновление токенов** - при истечении access token автоматически запрашивается новый
- **Реактивные формы** - валидация через Vee-Validate
- **Роутинг с защитой** - приватные маршруты требуют аутентификации
- **Обработка ошибок** - централизованная обработка через axios interceptors

### Backend

- **Command/Query разделение** - отдельные сервисы для чтения и записи
- **Dependency Injection** - через Spring IoC контейнер
- **Transaction Management** - автоматическое управление транзакциями через Spring
- **Exception Handling** - глобальная обработка исключений через `@ControllerAdvice`
- **Async обработка результатов** - вычисление оценки выполняется асинхронно

## 📄 Лицензия

Проект создан в рамках курса Naumen Java.

## 👥 Автор

Разработано для курса Naumen Java.

